# include<stdio.h>

int main() {
    int length;
    printf("Enter length:");
    scanf("%d", &length);

    int breadth;
    printf("Enter breadth:");
    scanf("%d", &breadth);
    
    int area = length * breadth;
    printf("Area of rectangle is:%d", area = length * breadth);
    return 0;
}